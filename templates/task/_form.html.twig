{# templates/task/_form.html.twig #}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">{{ 'Update' if form.vars.value.id else 'Create' }}</button>
{{ form_end(form) }}
